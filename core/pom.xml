<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>pfg.kraken</groupId>
  <artifactId>kraken</artifactId>
  <version>1.2.2</version>

  <packaging>jar</packaging>

  <name>Kraken : a tentacle-based pathfinding library for nonholonomic robotic vehicles</name>

  <url>https://github.com/PFGimenez/The-Kraken-Pathfinding</url>

  <properties>
      <maven.compiler.source>1.8</maven.compiler.source>
      <maven.compiler.target>1.8</maven.compiler.target>
      <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>

    <dependency>
      <groupId>pfg.injector</groupId>
      <artifactId>dependency-injector</artifactId>
      <version>[1.1,)</version>
    </dependency>

    <dependency>
      <groupId>pfg.config</groupId>
      <artifactId>config</artifactId>
      <version>[1.3,)</version>
    </dependency>

    <dependency>
      <groupId>pfg.graphic</groupId>
      <artifactId>graphic-toolbox</artifactId>
      <version>[1.0,)</version>
    </dependency>

    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.12</version>
      <scope>test</scope>
    </dependency>

  </dependencies>

  <build>
  </build>

</project>
